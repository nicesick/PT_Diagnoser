package com.example.demo;

import javax.servlet.http.HttpSession;

public class Util {
	/**
	 * getSessionValue
	 *
	 * @param String sKey @return String sValue @throws
	 */
	/*
	 * public static String getSessionValue(String sKey) { String sValue = "";
	 * 
	 * HttpSession sessionInfo = SessionUtil.getSession();
	 * 
	 * if (sessionInfo != null && sessionInfo.getAttribute(sKey) != null) { sValue =
	 * (String) sessionInfo.getAttribute(sKey); }
	 * 
	 * return sValue; }
	 */
}
